# JOB-09 v0.2.7 Source Change Summary

Baseline: v0.2.6 Selection / Spawn Repair.

## Lua changes

File: `lua/ge/extensions/redfoxTowRecoveryDispatch.lua`

- Version updated to 0.2.7.
- Settings schema updated to 3 through merge-safe migration.
- Added saved settings:
  - `dispatchVarietyEnabled = true`
  - `recentVehicleExactWindow = 6`
  - `recentVehicleModelWindow = 2`
  - `standardTowPassengerChance = 0.82`
  - `standardTowHeavyChance = 0.12`
  - `standardTowBusChance = 0.04`
- Added session caching for the eligible installed-vehicle catalog.
- Added per-Career `recentDispatchSelections` state without changing the existing state-file path.
- Added weighted single-target selection by call type.
- Added three-pass anti-repeat selection:
  1. exact and model both fresh;
  2. exact configuration fresh;
  3. unrestricted only when no fresh candidate exists.
- Fallback vehicle classes are checked before same-model repetition is allowed.
- Successful event targets are recorded in recent selection state.
- Added WE UI controls for anti-repeat enablement and repeat-window sizes.
- Added one-time `[RedFox][TOW][SPAM_AUDIT]` startup log.

## Static audit

- Lua compile: PASS.
- JSON parse: PASS.
- Protected Career/gameplay path scan: PASS.
- No `log()` in `M.onUpdate`: PASS.
- No direct save write in `M.onUpdate`: PASS.
- One-second scheduler gate preserved: PASS.
- Eight-second route refresh guard preserved: PASS.

## Not changed

- No physical semi/trailer auto-coupling yet.
- No convoy AI, fleet teleport, fire/pileup calls, hazmat, or cross-map cargo work.
- PSA mod not supplied and not modified.
