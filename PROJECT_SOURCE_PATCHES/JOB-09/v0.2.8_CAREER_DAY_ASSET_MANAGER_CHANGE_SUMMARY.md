# JOB-09 v0.2.8 — Source Change Summary

**Baseline:** v0.2.7 RLS Progression / Personal Claims  
**Candidate:** v0.2.8 Career-Day Clock / Organized Asset Manager  
**Status:** BUILT — RUNTIME UNTESTED

## Main Lua changes

File:

```text
lua/ge/extensions/redfoxTowRecoveryDispatch.lua
```

### Clock

- State schema increased from 3 to 4.
- Added saved `profile.careerClock` data.
- Added time-of-day observation through `core_environment.getTimeOfDay()`.
- Added map `dayLength` use when available.
- Added active unpaused simulation-time fallback.
- Added configurable `fallbackCareerDayMinutes` with default 30.
- Added legacy yard-record clock migration.
- Added Career-day record stamps for new abandoned, unpaid, and police impounds.
- Replaced `eligibleAt` real-time checks with Career-day eligibility checks.
- Replaced real-time storage-day calculations with completed Career days.

### Main-window organization

- Added `runtime.mainPage`.
- Added Dispatch, Fleet, Storage / Impound, and History page buttons.
- Removed simultaneous expansion of all four large sections.

### Fleet Book

- Added location filtering.
- Added selected-unit dropdown.
- Added assigned-location display and current-map assignment.
- Added Delivery / Box Truck role.
- Added Transport Tractor / Carrier role.

### Storage / impound

- Added map filter.
- Added status filter.
- Added selected-vehicle dropdown.
- Added fractional hold progress and remaining-day display.
- Added billable-day display.
- Added clear locked/eligible claim state.
- Added valuation-source display.
- Added selected-record reappraisal.

### History

- Replaced the expanded newest-20 list with a catalog dropdown and selected-record dropdown.

### Value selection

New target appraisal preference:

```text
marketValue
configBaseValue
Value/value
fallback 5000
```

Legacy records remain unchanged until explicitly reappraised.

### Public API

Added:

```text
getAssetManagerData()
```

Returns:

- Career clock;
- Fleet Book records;
- stored vehicle records;
- Tow History;
- yard locations by map.

## Metadata and reports

- Updated module/package version to 0.2.8.
- Added v0.2.8 scope, verification, inventory, file-tree, and testing files.
- Added `_redfox_dev_notes/PATCH_v0_2_7_TO_v0_2_8.diff`.

## Protected boundaries

No files were added beneath stock/RLS Career override paths. No RLS source or asset was copied into the package. Company ownership transfer and company-account writes remain intentionally absent.